[package]
name = "paper1_common"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_abi = { path = "../../../engine_abi" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.10"
